services:
  - type: web
    name: kisansathi
    runtime: node
    plan: free
    region: oregon
    buildCommand: npm run install:all && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 24.0.0
      - key: PORT
        value: 10000
